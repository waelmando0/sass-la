## install next last version

- pnpm dlx create-next-app@rc --turbo

- pnpm install babel-plugin-react-compiler

## Run the shadcn-ui init command to setup your project:

- pnpm dlx shadcn-ui@latest init
